# Readiness

Status: active technical component readiness report
Updated: 2026-08-14

## Purpose

Track the distance between the validated current state and the accepted NorthStar target in a form that is directly usable for autonomous planning.

This file is the execution bridge between:

- `NORTHSTAR.md` as the target contract
- `STATE.md` as the validated as-is picture
- `ROADMAP.md` as the ordered queue derived from readiness

If autonomous planning becomes ambiguous, this file decides:

- which component is actually closest to the target
- which blocker has the highest downstream leverage
- which bounded package should move next by default

## Maturity scale

- `4/4` stable for the current target and governed by specs/tests/evidence
- `3/4` strong and usable, but still carrying bounded debt or missing one important proof
- `2/4` partially aligned; meaningful blocker still controls the next step
- `1/4` scoped but not execution-ready

## Autonomous planning rule

Choose the next package by walking this order:

1. pick the highest-priority component whose blocker is still real in current evidence
2. prefer the blocker that unlocks more than one downstream component
3. prefer a package that can be bounded by smoke coverage, runtime evidence, or both
4. do not jump to a lower component only because it is easier or more available

## Component maturity

| ID | Component | Maturity | NorthStar role | Depends on | Current evidence | Main blocker | Default next bounded package | Done signal |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| DOC-1 | Operator-facing documentation contract | 3/4 | Keep the project operator-legible and handoff-safe | none | `NorthStar/State/Readiness/Roadmap` exist, core `DIRECTORY.md` coverage exists, and workflow docs are already smoke-guarded. | Planning still depends too much on reading several files together unless `READINESS` stays execution-grade. | Keep `READINESS`, `ROADMAP`, and the active index synchronized so the next package is inferable without operator intervention. | Next bounded package is obvious from docs alone, with no cross-reading required. |
| OBS-1 | Server-side request/response observability | 3/4 | Preserve structured, auditable runtime truth | DOC-1 | Bounded `rpc_received` and `rpc_response_sent` audit coverage exists on active `/mcp` paths, including authenticated local validation. | Observability is good enough for diagnosis, but still needs disciplined use as the source for client-entry decisions. | Use live trail evidence before adding any broader logging or diagnostics churn. | Active client-entry questions can be answered from existing bounded observability without new logging work. |
| AUTH-1 | OAuth21 authorized runtime | 4/4 | Keep the internal authorized surface production-grade and stable | DOC-1 | Resource-server identity, throttling, oversized-body aborts, bounded DCR growth, SQLite durability, canonical PKCE validation, secret-free rejection audits, and transaction-bound startup pruning are live at `server_start_id = 2026-07-29T19:28:19.541Z`. The first live prune found no candidates and zero orphan tokens; the final-load restart respected the daily interval; own-connector callability survived both restarts. Hermetic official `@modelcontextprotocol/client@2.0.0` coverage now independently proves DCR, PKCE S256, host-side `state`, RFC 9207 `iss`, issuer-stamped persistence, authenticated list/call in both protocol eras, process-restart recovery of the registered client plus active tokens from SQLite, legacy-path refresh-token rotation after that restart and an access rejection, and modern reuse of the rotated credentials. | No active auth blocker remains. | Hold auth steady; reopen only on a reproduced defect, current standards delta, or approved target change. | Durable state survives restart, startup maintenance is fail-closed and auditable, and connector calls remain authorized. |
| MEM-1 | Persistent memory retrieval quality | 4/4 | Preserve cross-session knowledge and make Polish/English retrieval reliable | DOC-1, AUTH-1 | Live PID `24132` at `server_start_id = 2026-08-01T18:52:13.024Z` reports `activation_ready=true` with protected token-file auth and explicit egress. EN-to-PL and PL-to-EN probes rank the intended entries first; token-file loss preserves lexical fallback; cache schema and binary probes show no plaintext; bounded backfill completed `64/64`, leaving all `67` active unique entries cached. Exact Unicode lexical tokens and bounded EN/PL stop words guard the ranking defect found by the live probe. | No active memory blocker remains. | Hold the accepted provider, cache, fallback, and ranking behavior steady; reopen only on measured regression or approved provider/model change. | Live memory save/search creates a non-plaintext cache, related PL/EN memory outranks unrelated memory, fallback remains functional during provider/cache failure, and no vectors/secrets appear in MCP output or audit. |
| SURF-1 | Connector-visible surface governance | 4/4 | Keep MCP-visible tools intentionally governed | DOC-1, AUTH-1 | Repository and live OAuth21 runtime align at `13 + 85 = 98`, fingerprint `ec7d3af5b4ea17f5`, `server_start_id = 2026-08-13T19:41:25.994Z`; R2 changed one input schema without adding a tool. | No active surface blocker remains. | Preserve the governed fingerprint and reopen only on measured enumeration, schema, annotation, cache, or direct-callability drift. | Count, schemas, annotations, lifecycle behavior, source safety, cache directives, and direct named callability align in one fresh client map. |
| PROC-1 | Process execution reliability | 4/4 | Support bounded long-running development commands with durable lifecycle evidence | DOC-1, AUTH-1, SURF-1 | `PROC-1B-R2` remains accepted and the repository now adds negotiated MCP Tasks as a thin `run_process` view over the same owner-bound SQLite/WAL registry. Clean-history validation proves durable task visibility before response, terminal result reconstruction, restart recovery under a new server instance, cross-owner denial, `tasks/get/update/cancel`, no `tasks/list`, and full `7 + 294`; the live OAuth21 `3008` runtime has not yet loaded this Tasks package. | No process-reliability blocker remains. Live Tasks acceptance is an operational load/probe boundary, not a reason to create another store or scheduler. | Preserve the Tasks/process/W3C correlation contract and move the internally actionable protocol queue to owner-bound process output artifacts. | One execution truth serves synchronous/custom-async/standard-Task views, survives restart, remains owner-isolated, and never persists or echoes raw argv/env. |
| CBM-1 | Codebase-Memory bridge and index integrity | 4/4 | Keep repository analysis trustworthy and mutation-safe | DOC-1, SURF-1 | Stdin native transport, duplicate change normalization, fail-fast runtime output configuration, operator-neutral executable fallback, fail-closed ADR snapshot gating, shared OAuth/HTTP helpers, v0.9.0 cache-schema repair, and snippet source-integrity validation are live at `server_start_id = 2026-07-29T19:28:19.541Z`. All three active indexes retain their counts; `manage_adr(get)` succeeds; `detect_changes` exposes exact totals and impact metadata; `ingest_traces` exposes runtime-edge status; semantic-only `search_graph` suppresses unfiltered structural `results`; path/project and Cypher caveats remain explicit; upstream reviews cover ordinals 1-277; and the local skill routes documentation/workflow questions to the knowledge index. Live `get_code_snippet` recovered verified `searchIndex` lines `1073-1084` while preserving incorrect native lines `898-909`; unrecoverable mismatches expose `source_reliable: false`. The July 28 strict stress run covered `663` calls across all fifteen `cbm_*` tools with `instability=[]`; current full offline smoke baseline is `7 + 298`. | No active CBM integrity blocker remains. | Preserve source-integrity checks, backup/migration tooling, stress coverage, upstream-review discipline, and the CBM-vs-knowledge-index boundary; reopen only on a reproduced regression, failed stress run, new upstream issue with local impact, or approved capability change. | Live identity remains governed with reliable snippet interpretation, repaired ADR storage, explicit partial-result metadata, and stable stress-tested results. |
| COMP-1 | Legacy `initialize` retirement readiness | 3/4 | Retire the main remaining protocol-debt path safely | OBS-1, AUTH-1, SURF-1 | The final MCP `2026-07-28` dual-era HTTP adapter is live and now regression-covered by the official `@modelcontextprotocol/client@2.0.0`: default legacy, automatic modern, and pinned modern clients all list and call tools, while server audit proves their actual entry paths. Fresh `2026-08-02` evidence on `server_start_id 2026-08-01T20:29:11.207Z` still shows `initialize_only` for operational `codex-mcp-client 0.146.0-alpha.9.2`, with `2` matching legacy entries and `0` `server/discover` entries. | Server capability is independently verified; real client-family entry behavior still blocks retirement. | Keep the official SDK regression, modern adapter, and legacy shim stable. Refresh operational evidence only after a newer client entry sample. | A fresh operational client family proves `server/discover` entry behavior or the remaining `initialize` dependence is explicitly accepted. |
| CTRL-1 | OAuth21 durable-state hygiene | 4/4 | Keep state maintenance safe and auditable | AUTH-1, DOC-1 | Explicit control-plane tools remain available for exceptional maintenance. Routine pruning now runs before RAM load at most daily under `BEGIN IMMEDIATE`, creates and verifies a logical rollback database before deletion, writes receipts, retains seven backup groups, and preserves state on backup/transaction failure. Initial live proof returned `no_candidates`; final-load proof at `2026-07-29T19:28:19.541Z` returned `interval_not_elapsed`. | No active maintenance blocker remains. | Preserve the startup boundary and exceptional manual rollback path; reopen on failed audit evidence or measurable store growth. | No RAM resurrection path, no orphan tokens, bounded backups, and deterministic audit status. |
| DEBT-1 | Session-bound outbound/sampling helper debt | 2/4 | Close residual local-helper debt without reopening target architecture | OBS-1 | Scope is explicitly recorded as bounded helper debt, not active route architecture. | No evidence yet that removing it outranks client-entry compatibility work. | Leave deferred until it blocks a higher-priority component or creates measurable execution drag. | Future work is either an intentional cleanup package or an explicit retention decision. |
| DOC-2 | Directory-level functional orientation | 3/4 | Make every high-value area self-describing for handoff/restart continuity | DOC-1 | Core operational directories, the high-churn CBM integration boundary, the high-churn `_workflow/operator_decisions` ledger, the project-local `using-codebase-memory` skill boundary, and the `docs/superpowers` plan/spec support boundary now have generator-owned or smoke-guarded maps. `scripts/audit_directory_docs.js` reports no missing `DIRECTORY.md` files among the top 25 tracked dirs with churn >= 5 in the last 30 days. | Repo-wide expansion would create churn unless it targets high-change directories. | Keep the churn-ranked guard at top25/churn>=5; extend `DIRECTORY` coverage only when a current audit exposes a real handoff gap. | Remaining high-churn documentation gaps are closed without repo-wide documentation sprawl. |
| RETR-1 | Workspace retrieval ergonomics | 4/4 | Reduce agent execution friction without changing product truth | DOC-1, SURF-1 | The refreshed live v3 knowledge index contains `279` documents, visited `1207` files and `51` directories, is not truncated, reports freshness `fresh`, extracts all `14` readiness rows, and links all `36` source-of-truth documents. Active-workflow, maturity, and `MEM-1` proof queries rank the authoritative documents first; regression coverage includes stale-source detection, long canonical rows, escaped/code pipes, identifier ranking, and proof-over-audit ordering. | No active retrieval blocker remains after the measured quality regression was repaired and live validated. | Preserve freshness-aware reads and the three acceptance queries; reopen on stale detection failure, incomplete canonical extraction, ranking regression, or measured scale pressure. | Current-state queries refuse silent staleness, structured workflow fields remain complete, and authoritative evidence ranks before related historical records. |
| FILE-1 | Structured large-file and Markdown mutation | 4/4 | Let weaker or context-limited agents edit exact file regions without whole-file transfer | DOC-1, SURF-1 | Owner-bound staged content, bounded inspection, streaming hash-preconditioned transforms, journaled source-preserving split/merge, and heading-aware Markdown operations are exposed as seven tools. Live connector E2E proved staging, append, section replacement, three-part split, and byte-identical merge on the 98-tool runtime. | No active file-operation blocker remains. | Preserve preview/receipt/hash requirements and reopen only on a reproduced integrity, recovery, usability, or scale defect. | Tools remain directly callable, reject stale inputs, preserve sources, recover journaled writes, and produce exact bounded results without whole-file model transfer. |
| OPS-1 | Operational E2E and soak coverage | 3/4 | Prove stable behavior across real operational boundaries, not only isolated feature smokes | OBS-1, AUTH-1, SURF-1 | `OPS-1A` adds an auditable matrix, an explicit bounded runner, and `56/56` green invocations: 50 hermetic plus three live Cloudflare and three live MCP network probes. Restart, cancellation, timeout, process persistence, network, and destructive rollback have classified evidence; the initial serverless network false failure is prevented by a dedicated isolated harness manifest. | Full operator-driven OAuth reconnect remains intentionally manual, and live SFTP is blocked by the missing post-reinstall SFTP config. | Preserve the matrix and runner. Reopen as `OPS-1B` only when live SFTP infrastructure is available, a reconnect incident creates decision-relevant evidence, or a bounded run reproduces a defect. | Live SFTP read-only evidence and a current full reconnect/recovery observation close the two remaining external boundaries without weakening owner, auth, or rollback controls. |

## Dependency spine

Read the queue through this dependency spine:

1. `DOC-1` documentation contract
   - keeps orientation durable
   - makes autonomous selection trustworthy
2. `OBS-1` observability
   - provides the truth source for runtime/client interpretation
3. `AUTH-1` authorized runtime
   - remains stable while client-entry evidence is event-gated
4. `SURF-1` surface governance
   - repository, live runtime, and refreshed model map are aligned at `98`; direct process and structured-file callability is accepted
5. `CBM-1` bridge and index integrity
   - is complete and remains a protected support layer
6. `MEM-1` persistent memory retrieval quality
   - live activation, measured PL/EN quality, fallback, confidentiality, and full active-memory backfill are complete
   - preserve as a 4/4 support layer and reopen only on reproduced evidence
7. `COMP-1` initialize retirement readiness
   - now has a live final `2026-07-28` adapter and is blocked only by operational clients that still enter through `initialize`
8. `PROC-1`
   - is accepted at 4/4 and acts as protected infrastructure for broader operational soak work
9. `OPS-1`
   - resumes after `PROC-1A` and remains the broad autonomous quality package while `COMP-1A` is externally blocked
9. `CTRL-1`, `DEBT-1`, `DOC-2`
   - operational support tracks; `DOC-2A` remains a bounded fallback after higher-value E2E work

## Current blockers by leverage

1. `COMP-1`
   Final modern-era server capability is live, but `codex-mcp-client 0.146.0-alpha.9.2` still enters through `initialize`. Retirement remains externally event-gated until newer operational traffic changes that fact.

2. `DOC-2`
   Directory coverage remains incomplete outside normalized operational areas. This is a bounded fallback only when one current high-churn gap is demonstrable.

`CBM-1`, `RETR-1`, `PROC-1`, and `SURF-1` have no active blocker. `OPS-1` is the active broader quality track.

## Autonomous execution packages

Active internally actionable selection is now `PROCESS-ARTIFACTS`, followed by `CIMD` and fixture-scoped `MRTR`. `MCP-TASKS-PROCESS-ADAPTER` and `TRACE-CONTEXT` are repo-complete protected regression evidence; their live-load proof remains a separate operational boundary. Event-gated `COMP-1A`, `OPS-1B`, and bounded fallback `DOC-2A` remain available without blocking the protocol queue.

| Package | Targets | Why now | Primary evidence source | Stop condition | If blocked |
| --- | --- | --- | --- | --- | --- |
| `PROC-1A/B` | `PROC-1`, `SURF-1`, `OBS-1` | Completed regression reference; durable phase B, PID-reuse recovery hardening, and refreshed connector enumeration are accepted. | `_workflow/operator_decisions/process_runner_async_live_acceptance.md`, `_workflow/operator_decisions/process_job_pid_reuse_recovery.md`, persistence/error/resolver smokes, external stress, official-client OAuth restart E2E, direct `process_list/events` calls | Reopen only on an independent adversarial finding or reproduced regression. | Preserve fail-closed owner binding, renewable instance leases, periodic orphan reconciliation, no persisted args/env, and no command replay. |
| `CBM-ADR-REPAIR` | `CBM-1` | Completed regression reference. | External backup manifest, bounded migrator, `cbm_manage_adr`, `cbm_index_status`, and isolated-cache reconstruction tests | Reopen only if ADR reads, schema compatibility, or preserved counts regress. | Restore from the external checkpoint before any further cache mutation. |
| `OAUTH-DUPLICATE-HELPER-REVIEW` | `CBM-1`, `AUTH-1` | Completed regression reference. | Shared helper modules, characterization tests, OAuth/runtime smokes | Reopen only if exact helper behavior diverges or a security fix exposes a new duplicate. | Stop before semantic refactoring if equivalence cannot be proved. |
| `FINAL-LIVE-LOAD` | `CBM-1`, `SURF-1`, `AUTH-1` | Completed regression reference. | Full offline suite, self-test, restart receipt `manual-1785110528926`, refreshed connector identity, and live CBM probes | Reopen only if live identity or hardened behavior regresses. | Preserve the current runtime and restore from the existing checkpoint before new live mutation. |
| `COMP-1A` | `COMP-1`, `OBS-1` | Highest-leverage remaining protocol gate, but externally event-gated after the August 1 refresh. | `_logs/.mcp-tests-audit.jsonl`, explicit live `server_start_id` or `--latest-entry-window`, and `_workflow/operator_decisions/initialize_client_compatibility_evidence.md` | Current operational client-family evidence is refreshed into a clear `initialize_only` or `server_discover_entry` verdict. | Fresh evidence still shows `initialize_only` for `codex-mcp-client 0.146.0-alpha.9.2`; preserve the live modern adapter and legacy shim until newer external traffic appears. |
| `OPS-1A` | `OPS-1`, `OBS-1`, `SURF-1` | Completed regression reference. | Classified matrix, bounded runner, durable job `2ed5a1f1-2695-4304-999c-17d1f7e0352e`, `56/56` green invocations, and closeout record | Reopen only when the runner reproduces a defect or a current evidence claim regresses. | Preserve evidence modes and never promote simulated coverage into a live claim. |
| `OPS-1B` | `OPS-1`, `AUTH-1` | Closes the two external boundaries left by OPS-1A. | Recovered live SFTP config or a real operator-driven reconnect event | Live read-only SFTP and current reconnect recovery evidence are recorded without weakening credentials or isolation. | Keep the boundary explicit and remain at `3/4`; do not request operator action only to manufacture coverage. |
| `COMP-1B` | `COMP-1`, `SURF-1` | Only worth doing after `COMP-1A` narrows the blocker or changes the decision boundary. | Fresh `COMP-1A` result plus `_workflow/operator_decisions/initialize_retirement_decision_prep.md` | A bounded retirement-decision delta exists or the blocker is restated with tighter current evidence. | Fall back to `SURF-1A` only if connector/UI truth is the deciding ambiguity. |
| `SURF-1A` | `SURF-1` | Connector/UI verification matters only when it changes the protocol-debt decision. | Codex/OpenAI UI evidence, runtime callability evidence, connector-facing tool inventory evidence | UI-visible enumeration is either confirmed decision-relevant or explicitly ruled irrelevant to the current package. | Return to `COMP-1A`; do not let UI verification become autonomous churn. |
| `DOC-2A` | `DOC-1`, `DOC-2` | Safe fallback package when higher-leverage protocol evidence is externally timing-bound. | `READINESS.md`, `ROADMAP.md`, `ACTIVE_WORKFLOW_INDEX.md`, current directory maps | One real high-churn documentation gap is closed and the next bounded package remains inferable from docs alone. | Stop if the work would become repo-wide documentation sprawl instead of a bounded orientation fix. |

## Completed hardened v0.9.0 rollout

The hardened CBM v0.9.0 rollout is complete.

- Live runtime: `85` tools, including fifteen `cbm_*` tools and `memory_update_task`; the already-open model task retains its startup-time `84`-tool map.
- Live identity: `server_start_id = 2026-08-02T09:21:24.828Z`, fingerprint `b435c9f507d18f12`, intentional additive task-lifecycle surface, final dual-era protocol behavior, and direct existing-tool callability after restart.
- Live behavior: stdin native transport emits no raw-JSON deprecation warning; `detect_changes` normalizes duplicate entries; volatile native `search took <ms>` warnings no longer leak into stable `cbm_search_code` result signatures; ADR reads succeed across all three repaired indexes.
- Delete contract: `project`, `confirm`, `state_handle`; the handle remains TTL-bound, auth-bound, project-bound, one-time, and audit-redacted.
- Post-refresh outcome: disposable index deleted; repeated delete through a fresh challenge mapped to `cbm_project_not_found`; source fixture directory remained intact.
- Restart/refresh state: runtime restart is complete; one model-surface re-enumeration remains pending before direct named `memory_update_task` acceptance.

## Default next package

Unless fresh evidence changes the dependency graph:

1. `COMP-1A` remains event-gated until traffic newer than the August 2 `codex-mcp-client 0.146.0-alpha.9.2` sample creates a meaningfully new window.
2. `DOC-2A` is the only bounded autonomous fallback when one real high-churn orientation gap is demonstrable.

## Latest supporting package

- `2026-08-02`: completed `MCP-OFFICIAL-SDK-V2-INTEROP`. Exact dev dependency `@modelcontextprotocol/client@2.0.0` now proves legacy default, automatic modern, and pinned `2026-07-28` behavior end-to-end on isolated servers; server audit assertions verify the entry paths. The OAuth21 extension independently proves SDK-driven DCR, PKCE S256, callback `state`/`iss`, issuer-bound persistence, authorized list/call in both eras, process-restart recovery from SQLite, post-restart legacy-path refresh-token rotation, and modern credential reuse. Full validation is `7 + 272`; no production runtime restart or connector refresh is required. Details: `_workflow/operator_decisions/mcp_official_sdk_v2_interop_closeout.md`.
- `2026-08-01`: completed `MCP-2026-DUAL-ERA`. The live `/mcp` route now separates final `2026-07-28` per-request behavior from legacy initialize-era behavior, validates standard mirrored headers, emits modern result envelopes and final protocol errors, persists DCR `application_type`, and preserves older clients through bounded compatibility rules. Full validation is `7 + 270`; controlled restart `manual-1785616149554` loaded the package at `server_start_id 2026-08-01T20:29:11.207Z`, and direct `workbench.get_info` succeeded without relogin. Fresh Codex `0.146.0-alpha.9.2` evidence still blocks initialize retirement. Details: `_workflow/operator_decisions/mcp_2026_07_28_dual_era_closeout.md`.
- `2026-08-01`: completed `RETR-1-QUALITY-REGRESSION`. The v3 knowledge index records content and directory-entry fingerprints and reports `fresh`, `stale`, or `unknown` without false staleness from identical generator rewrites; canonical workflow extraction uses a complete but bounded transient sample while persisted excerpts remain bounded. Robust table parsing preserves escaped and code-span pipes, and identifier/proof-aware ranking makes current authoritative evidence outrank historical audits. Targeted, schema, full offline, controlled-restart, and live acceptance checks passed without OAuth relogin; details are in `_workflow/operator_decisions/retr_1_quality_regression_closeout.md`.
- `2026-07-29`: completed `UPSTREAM-PATTERN-LAB` as a reusable upstream extraction workflow. Five additional repos were cloned into the ignored local corpus beside existing `codebase-memory-mcp` and `obsidian-mcp-server` samples: `doctree-mcp`, `repo-graphrag-mcp`, `docs-mcp-server`, `graph-mem-mcp`, and `markdown-rag-mcp`. `scripts/extract_upstream_repo_patterns.js` now extracts file roles, package scripts, Make targets, retrieval/graph/parser/storage/update/MCP signals, evidence samples, and bounded transplant candidates; `docs/UPSTREAM_REPO_PATTERN_LAB.md` records the first seven-repo report; `_tests/smoke_upstream_repo_pattern_extractor.js` guards the extractor on a hermetic fixture.
- `2026-07-29`: completed the upstream second pass and `CBM-SNIPPET-INTEGRITY` package. The pattern lab records exact nested-repository pins, dependency manifests and costs, source-level definition evidence, and explicit second-pass decisions. Native CBM v0.9.0 returned `searchIndex` source from lines `898-909` after a fresh reindex although the declaration was at lines `1073-1084`; the bridge validates requested-symbol presence, recovers only from bounded verified workspace files, preserves native spans, and marks unrecoverable source with `source_reliable: false`. Controlled restart `manual-1785348037500` loaded the package at `server_start_id = 2026-07-29T18:00:39.162Z`; the live connector returned corrected lines `1073-1084`, `source_integrity: bridge_recovered`, and `source_reliable: true`.
- `2026-07-29`: completed a bounded `DOC-2A` fallback after `COMP-1A` stayed blocked by operational `initialize_only` evidence. `docs/superpowers`, `docs/superpowers/plans`, and `docs/superpowers/specs` now have generator-owned `DIRECTORY.md` maps, and `_tests/smoke_directory_docs_audit.js` now guards the top 25 churn-ranked directories with churn >= 5. Validation: `node scripts/audit_directory_docs.js --since="30 days ago" --limit=25 --min-churn=5 --fail-on-missing` reports `missing: 0`; generator and audit smokes are GREEN.
- `2026-07-29`: bounded OAuth21/PKCE audit found that the shared RFC 7636 grammar accepted non-canonical `S256` challenge shapes even though the server supports only `S256`. The authorization server now requires exactly 43 base64url characters for `code_challenge`, keeps the verifier at `43..128` unreserved characters, and audits PKCE rejection reasons without raw verifier/challenge values. Targeted flows and full offline `7 + 268` smokes are GREEN. Restart `manual-1785349273914` loaded the change at `server_start_id = 2026-07-29T18:21:15.570Z`; a live 44-character challenge returned `code_challenge_invalid`, its audit record contained no challenge/verifier value, and the own `workbench` connector remained callable.
- `2026-07-29`: completed the repository and keyword-fallback live-load portion of `MEM-1`. The implementation keeps JSONL authoritative, stores no plaintext in its optional SQLite vector cache, requires explicit provider/egress/token configuration, bounds external calls, validates 1024-dimensional vectors, and falls back to keyword search on provider or cache failure. Restart `manual-1785351228729` loaded the package; health reports 84 tools and own-connector memory calls succeed.
- `2026-08-01`: closed the descriptor review and the MEM-1 runtime-configuration presence audit. Current Codex fetched the live descriptor twice, and `scripts/audit-memory-embedding-runtime.ps1` proved that live PID `444` has no provider, egress opt-in, token, or timeout override and no cache; no secret value was emitted. Activation remains safely blocked until the secret-file package and credential provisioning are complete.
- `2026-08-01`: completed `MEM-1` live activation and acceptance. PID `24132` reports `activation_ready=true`; live EN-to-PL and PL-to-EN probes rank the intended entries first; controlled token-file loss preserves lexical fallback; exact Unicode tokens plus bounded EN/PL stop words fix the ranking defect exposed by the first probe; and bounded backfill completed all `64` missing vectors for `67/67` active unique memories without plaintext, vector, or secret output.
- `2026-07-29`: completed the `RETR-1` active-workflow ranking pass. The knowledge index now detects planning/workflow intent and ranks `_workflow/ACTIVE_WORKFLOW_INDEX.md`, `_workflow/READINESS.md`, and `_workflow/ROADMAP.md` ahead of historical `_workflow/operator_decisions` records for natural "what next / why blocked" queries. Validation: `_tests/smoke_build_index_tool.js`, `_tests/smoke_output_schema_guard.js`, full `node _tests/run_all_smokes.js --skip-network`, live restart `manual-1785343566402`, and live `workbench` `search_index_context`/`collect_context` probes are GREEN.
- `2026-07-29`: extended `RETR-1` from flat metadata toward a lightweight deterministic document graph inspired by local GraphRAG/documentation-memory patterns. `knowledge_summary.document_graph` now reports internal doc references, linked-node coverage, top linked docs, active entrypoint outgoing links, source-of-truth link coverage, and unresolved-reference samples without adding external databases or embedding dependencies. Validation: `_tests/smoke_build_index_tool.js`, `_tests/smoke_output_schema_guard.js`, `_tests/smoke_schema_compat.js`, `_tests/smoke_workflow_state_orientation_map.js`, and full `node _tests/run_all_smokes.js --skip-network` are GREEN.
- `2026-07-29`: reviewed upstream `DeusData/codebase-memory-mcp` issues 201-277. Report: `docs/CBM_UPSTREAM_ISSUES_201_277_REVIEW.md`. Local action: project skill/reference guidance now preserves the CBM-vs-knowledge-index boundary by routing documentation, workflow, reports, specs, ADR text on disk, and `DIRECTORY.md` orientation questions to `workbench` `profile=knowledge`, while keeping CBM for indexed code graph work. Targeted skill and directory smokes are GREEN.
- `2026-07-28`: extended the documentation-first workspace index from facets into workflow extraction. `knowledge_summary.workflow_summary` now reports canonical workflow document presence, server-spec counts, runtime identity from large/truncated `state.json` prefixes, workflow progress markers, parsed `READINESS.md` components, parsed `ROADMAP.md` priority items, health flags, and documentation gaps. Validation: scoped `mcp-tests` knowledge build remained `truncated=false` with `247` indexed documents and extracted `10` readiness components, `6` roadmap priorities, `29` server specs, authenticated total `84`, `next_primary=comp-1a-on-fresh-external-client-traffic`, and `documentation_gaps=[]`. `_tests/smoke_build_index_tool.js`, `_tests/smoke_output_schema_guard.js`, and `_tests/smoke_schema_compat.js` guard the expanded contract.
- `2026-07-28`: redesigned the workspace retrieval index into a documentation-first knowledge map instead of a mixed code grep. `build_index` now accepts `profile=knowledge|source|all`; `knowledge` is the default and indexes documentation plus structured project-truth files while excluding code-like files and noisy control-plane records. `search_index`, `search_index_context`, `collect_context`, and `collect_romionsim_context` expose shared retrieval metadata (`index_scope`, `path_filter`, `index_profile`, `index_truncated`, `index_created_at`, `index_count`) and per-result document metadata (`title`, `kind`, `authority`, `format`). `build_index` and `index_status` expose `knowledge_summary` facets and top authority documents. Validation: scoped `mcp-tests` knowledge build completed with `truncated=false`, `247` indexed documents, `1099` visited files, `852` extension skips, and source-of-truth ranking now favors `NORTHSTAR`, `STATE`, `READINESS`, `ROADMAP`, workflow canon, and `SERVER_*_SPEC.json`; full offline smoke remains GREEN at `7 + 267`. `_tests/smoke_build_index_tool.js`, `_tests/smoke_output_schema_guard.js`, and `_tests/smoke_schema_compat.js` guard the contract.
- `2026-07-28`: refreshed `COMP-1A` with the latest-entry-window path. The latest runtime slice `2026-07-28T16:05:12.562Z` is stale entry evidence, but `node _workflow/scripts/client_entry_path_report.js --latest-entry-window --client-name=codex-mcp-client --evidence-scope=operational --max-age-days=2 --limit=20` selects `server_start_id 2026-07-28T03:49:00.666Z` and reports `codex-mcp-client 0.146.0-alpha.3.1` as `initialize_only` with `2` successful legacy responses and `0` `server/discover` entries. `_tests/smoke_client_entry_path_report_script.js` guards the new selection mode.
- `2026-07-28`: completed a second bounded `DOC-2A` support package for project-local CBM skill orientation and future task selection. `scripts/audit_directory_docs.js` ranks tracked high-churn directories by `DIRECTORY.md` coverage; `.agents/skills/using-codebase-memory/DIRECTORY.md` and `.agents/skills/using-codebase-memory/references/DIRECTORY.md` are now generator-owned; `_tests/smoke_directory_docs_audit.js` guards no missing maps in the top 20 dirs with churn >= 7. Full offline smoke is GREEN at `7 + 267`.
- `2026-07-28`: completed one bounded `DOC-2A` fallback for the high-churn `_workflow/operator_decisions` ledger. `scripts/generate_directory_docs.js` now owns `_workflow/operator_decisions/DIRECTORY.md`; `_tests/smoke_directory_docs_generator.js` guards the updated decision-ledger groups and active-queue boundary. `ACTIVE_WORKFLOW_INDEX.md` was also synchronized to the current CBM live identity `server_start_id = 2026-07-28T16:05:12.562Z`.
- `2026-07-28`: reviewed upstream `DeusData/codebase-memory-mcp` issues 151-200. Report: `docs/CBM_UPSTREAM_ISSUES_151_200_REVIEW.md`. Local actions: whitespace path/project caveats for `cbm_index_repository`, `cbm_search_code`, and `cbm_get_code_snippet`; trace-path empty-result interpretation guidance; docs directory inventory update. Targeted smokes and live connector probe are GREEN at `server_start_id = 2026-07-28T16:05:12.562Z`.
- `2026-07-28`: reviewed upstream `DeusData/codebase-memory-mcp` issues 101-150. Report: `docs/CBM_UPSTREAM_ISSUES_101_150_REVIEW.md`. Local actions: Windows non-ASCII index-path and project-identifier caveats for `cbm_index_repository`, `cbm_search_code`, and `cbm_get_code_snippet`; skill/tool-reference updates; docs directory inventory update. Targeted smokes and live connector probe are GREEN at `server_start_id = 2026-07-28T15:43:52.823Z`.
- `2026-07-28`: reviewed upstream `DeusData/codebase-memory-mcp` issues 51-100. Report: `docs/CBM_UPSTREAM_ISSUES_51_100_REVIEW.md`. Local actions: broader source-bearing excluded-route detection, Windows non-ASCII `search_code` caveats, unsupported Cypher shape caveats, and resident-set discovery guidance. Targeted smokes, self-test, full offline smoke, restart `manual-1785252329171`, and live connector probes are GREEN at `server_start_id = 2026-07-28T15:25:30.678Z`.
- `2026-07-28`: reviewed the first 50 open upstream `DeusData/codebase-memory-mcp` issues and comments. Report: `docs/CBM_UPSTREAM_ISSUES_FIRST50_REVIEW.md`. Local bridge actions: semantic-only `search_graph` suppresses unfiltered structural `results` while preserving `semantic_results`; `index_repository` warns on source-bearing excluded route directories such as `pages/api/assets`; specs and skill guidance were updated. Targeted smokes, full offline smoke, self-test, restart `manual-1785214492092`, and live connector probes are GREEN at `server_start_id = 2026-07-28T04:54:53.586Z`.
- `2026-07-28`: tightened CBM agent guidance and live partial-result regression coverage. `.agents/skills/using-codebase-memory/SKILL.md` now instructs agents to prefer structured `bridge_analysis`, `impact_resolution_reason`, and `runtime_edge_creation` fields over warning prose; `_tests/smoke_cbm_agent_skill.js` guards those markers; `_tests/smoke_cbm_live_bridge_stress.js` now exercises live `cbm_detect_changes` and `cbm_ingest_traces` metadata. Targeted smokes and full offline smoke are GREEN at `7 + 266`; no connector-visible surface change was made.
- `2026-07-28`: completed manual CBM bridge sample-repo stress closeout. `_tests/stress_cbm_bridge_samples.js` validates all exposed CBM bridge tools repeatedly across selected `_repos_with_code_samples`; the strict default four-repo run recorded `663` exact calls across all fifteen `cbm_*` tools, including lifecycle `index/delete`, with `instability=[]`, and earlier large-repo stress stayed stable after fixing volatile `cbm_search_code` warning normalization. Full offline smoke is GREEN at `7 + 266`; no connector-visible surface change was made.
- `2026-07-28`: live-loaded CBM partial-result metadata on OAuth21 `3008` at `server_start_id = 2026-07-28T04:54:53.586Z`. `detect_changes` exposes returned/omitted totals, `bridge_analysis`, and `impact_resolution_reason`; `ingest_traces` exposes `runtime_edge_creation` and `runtime_edges_created`; semantic-only `search_graph` suppression and source-bearing excluded-route warnings are live. Live probes confirmed the new fields; no connector refresh was required.
- `2026-07-27`: refreshed `COMP-1A` against explicit live `server_start_id 2026-07-27T03:10:26.042Z`. Operational `openai-mcp 1.0.0` remains `initialize_only` with `8` successful responses and `0` `server/discover` entries. The shared child-server helper now isolates audit output, and the report selects and attributes a named live window without deleting historical test evidence.
- `2026-07-27`: completed one bounded `DOC-2A` fallback for the high-churn CBM integration boundary. `scripts/generate_directory_docs.js` now owns `src/integrations/DIRECTORY.md` and `src/integrations/codebase_memory/DIRECTORY.md`; generator and operator-contract guards preserve the map and its repository/runtime/index truth boundary.
- `2026-07-27`: closed test-harness control-state pollution. Self-test no longer starts the restart controller or writes tool-surface state; every smoke child-server now uses hermetic surface/restart/rate paths; the restored operational `tool-surface-state.json` remains at 84 tools and `server_start_id = 2026-07-27T03:10:26.042Z` across standalone harnesses, self-test, and the full suite. No additional restart was required.
- `2026-07-27`: completed final live load for CBM reliability hardening and native-cache repair. The refreshed runtime exposes stdin transport, duplicate change normalization, fail-fast startup configuration, operator-neutral executable fallback, fail-closed ADR snapshot gating, shared OAuth/HTTP helpers, and repaired v0.9.0 cache schemas at unchanged 84-tool identity. Live delete verification returned `deleted` then `cbm_project_not_found`; full offline smoke is GREEN at `7 + 266`. Report: `docs/CBM_RELIABILITY_HARDENING_REPORT.md`.
- `2026-07-26`: completed the initial live CBM v0.9.0 surface rollout. The refreshed connector exposed `state_handle`; two-phase deletion returned `deleted`; a fresh second challenge returned `cbm_project_not_found`; `_tests/fixtures/cbm-live-fixture` remained intact; the historical combined fingerprint was `6a1329e3b3892b9c`.
- `2026-07-26`: completed the isolated fresh-cache v0.9.0 CBM rebaseline across `mcp-tests`, `papers-memory-mcp`, and `autonomous_llm_handbook`; report: `docs/CBM_V0_9_0_REBASELINE_REPORT.md`. Native path scoping, recursion handling, ambiguity handling, and concurrency improved; bridge compensations now cover output bounding, scope enforcement, ADR restoration, trace placeholder warnings, and project-not-found classification.
- `2026-07-17`: read-only remote-site flows (`list`, `read`, runtime status, retention preview) no longer create remote ops directories as a side effect. Guarded by `smoke_remote_site_lifecycle` and the full `run_all_smokes --skip-network` suite.
- `2026-07-17`: refreshed `COMP-1A` evidence still shows live OAuth21 `/mcp` current-window traffic as `initialize_only` for `codex-mcp-client 0.145.0-alpha.18`, with the retained blocker already present in `1d` and `2d` operational windows.
- `2026-07-17`: completed `DOC-2A` by extending bounded directory-map coverage into the runtime-owned control-plane backup roots and live prune bundle docs, while keeping the next package inferable from workflow docs alone.

## Current readiness rule

Do not treat a maturity score as permission to remove compatibility paths without:

- repo evidence
- live runtime evidence
- client/connector evidence
- smoke coverage
