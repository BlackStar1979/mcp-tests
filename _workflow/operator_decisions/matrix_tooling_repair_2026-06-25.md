# Matrix tooling repair

Status: GREEN CANDIDATE / NO RUNTIME CHANGE

Added matrix check script and run-all guard. Added event catalog root spec. Updated runtime config flag classification. Matrix check guards root spec mapping, env/route coverage, flag classification, and event catalog coverage.
