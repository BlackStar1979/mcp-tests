# Retired root backups

This directory is an archival quarantine for legacy root-level backup material.

A grep hit in this directory is archival evidence first, not current implementation guidance.

Active project truth remains in the current workflow index, canon, state orientation map, source tree, and root specs.
