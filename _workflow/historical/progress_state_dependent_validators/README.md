# Retired progress-state dependent validators

These files were moved out of active test/script locations because they depend on the removed workflow progress fields in `_workflow/state.json`. They are preserved only as historical validation artifacts and are not current machine authority.
