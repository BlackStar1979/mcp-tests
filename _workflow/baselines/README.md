# Runtime Baselines

`stage8_frozen_runtime_baseline.json` is the current frozen baseline file retained for compatibility.

The filename is historical. The manifest content may be re-frozen by later explicit baseline-change stages. The current source of truth is the manifest content, not the literal Stage 8 filename.

Governance references:

```text
_workflow/longterm/stage10_baseline_update_and_release_discipline.md
_workflow/longterm/stage12_step5b_server_version_correction_0_29_0.md
```
