# Archived Non-Run-All Stale Tests

This directory holds top-level `_tests/*.js` files that were removed from the current review surface on `2026-06-29`.

They are retained only as historical artifacts or rewrite candidates.

Do not treat these files as current validation coverage.

Before restoring any file to the top-level `_tests` directory, rewrite it against current canonical specs and current workflow/state structure.
