# src/plugin

## Purpose

Reserved target for future plugin subsystem implementation modules. Runtime plugin discovery still uses `plugins/` for manifest-backed candidates; test-only templates live under `_tests/fixtures/plugins`.

## Files

- `README.md` — directory orientation and future extraction target definition.

## Subdirectories

- none
