# src/schemas

## Purpose

Schema definitions and response-shape contracts used by runtime and guards.

## Files

- `dev_tools.js` — JavaScript module or executable smoke/helper script in this area.
- `fs_tools.js` — JavaScript module or executable smoke/helper script in this area.
- `net_tools.js` — JavaScript module or executable smoke/helper script in this area.
- `observability_tools.js` — JavaScript module or executable smoke/helper script in this area.
- `plugin_catalog_tools.js` — JavaScript module or executable smoke/helper script in this area.
- `plugin_execution_tools.js` — JavaScript module or executable smoke/helper script in this area.
- `plugin_registry_tools.js` — JavaScript module or executable smoke/helper script in this area.
- `plugin_visibility_tools.js` — JavaScript module or executable smoke/helper script in this area.
- `README.md` — Directory orientation maintained by the semantic README writer.
- `runtime_status.js` — JavaScript module or executable smoke/helper script in this area.
- `session_toolset_tools.js` — JavaScript module or executable smoke/helper script in this area.

## Subdirectories

- none
