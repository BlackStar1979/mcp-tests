# src

## Purpose

Active TEST MCP source modules. Runtime code and policy helpers live here; root-level auth/schemas/util are forbidden.

## Files

- `audit_export_redactor.js` — JavaScript module or executable smoke/helper script in this area.
- `audit_export_safety.js` — JavaScript module or executable smoke/helper script in this area.
- `audit_redaction_integration_plan.js` — JavaScript module or executable smoke/helper script in this area.
- `auth_bearer_cutover_guard.js` — JavaScript module or executable smoke/helper script in this area.
- `auth_bearer_dry_run.js` — JavaScript module or executable smoke/helper script in this area.
- `auth_modular_parity.js` — JavaScript module or executable smoke/helper script in this area.
- `auth_transition.js` — JavaScript module or executable smoke/helper script in this area.
- `descriptor_audit.js` — JavaScript module or executable smoke/helper script in this area.
- `list_changed_audit_receipt.js` — JavaScript module or executable smoke/helper script in this area.
- `list_changed_harness.js` — JavaScript module or executable smoke/helper script in this area.
- `list_changed_notification_bus.js` — JavaScript module or executable smoke/helper script in this area.
- `list_changed_pipeline.js` — JavaScript module or executable smoke/helper script in this area.
- `observability_redaction_summary.js` — JavaScript module or executable smoke/helper script in this area.
- `observability_status.js` — JavaScript module or executable smoke/helper script in this area.
- `output_schema_guard.js` — JavaScript module or executable smoke/helper script in this area.
- `plugin_catalog.js` — JavaScript module or executable smoke/helper script in this area.
- `plugin_execution.js` — JavaScript module or executable smoke/helper script in this area.
- `plugin_policy.js` — JavaScript module or executable smoke/helper script in this area.
- `plugin_readonly_handlers.js` — JavaScript module or executable smoke/helper script in this area.
- `plugin_registry.js` — JavaScript module or executable smoke/helper script in this area.
- `plugin_visibility_state_store_apply_gate.js` — JavaScript module or executable smoke/helper script in this area.
- `plugin_visibility_state_store_pipeline.js` — JavaScript module or executable smoke/helper script in this area.
- `plugin_visibility_state_store_preview.js` — JavaScript module or executable smoke/helper script in this area.
- `plugin_visibility_state_store_receipt.js` — JavaScript module or executable smoke/helper script in this area.
- `plugin_visibility_state.js` — JavaScript module or executable smoke/helper script in this area.
- `plugin_visibility.js` — JavaScript module or executable smoke/helper script in this area.
- `README.md` — Directory orientation maintained by the semantic README writer.
- `runtime_status.js` — JavaScript module or executable smoke/helper script in this area.
- `schema_compat.js` — JavaScript module or executable smoke/helper script in this area.
- `security_boundary.js` — JavaScript module or executable smoke/helper script in this area.
- `security_first_preflight.js` — JavaScript module or executable smoke/helper script in this area.
- `session_toolset.js` — JavaScript module or executable smoke/helper script in this area.
- `stage_metadata.js` — JavaScript module or executable smoke/helper script in this area.
- `state_store_apply_readiness_gate.js` — JavaScript module or executable smoke/helper script in this area.
- `tool_loader.js` — JavaScript module or executable smoke/helper script in this area.
- `tool_policy.js` — JavaScript module or executable smoke/helper script in this area.
- `tools_list_diff.js` — JavaScript module or executable smoke/helper script in this area.

## Subdirectories

- `auth` — Authentication modes, transition guards, and auth policy helpers.
- `exec` — Reserved target for future execution wrapper internals.
- `plugin` — Reserved target for future plugin subsystem implementation split.
- `runtime` — Reserved target for future server.js container extraction.
- `schemas` — Shared JSON-schema and output-shape contracts.
- `util` — Shared policy, path, network, and code-workspace utilities.
