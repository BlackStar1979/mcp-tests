{
  "name": "mcp-tests",
  "version": "0.40.0",
  "private": true,
  "description": "TEST MCP workbench for connector/runtime contract validation.",
  "type": "commonjs",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "ssh2-sftp-client": "^12.0.1"
  },
  "scripts": {
    "docs:directory:audit": "node scripts/audit_directory_docs.js",
    "docs:directory": "node scripts/generate_directory_docs.js",
    "start": "node server.js",
    "self-test": "node server.js --self-test",
    "test": "node _tests/run_all_smokes.js --skip-network",
    "test:full": "node _tests/run_all_smokes.js"
  },
  "allowScripts": {
    "ssh2@1.17.0": true
  }
}
